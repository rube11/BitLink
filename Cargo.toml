[package]
name = "bit-to-byte"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "A readable terminal messenger using the club UDP relay"
publish = false
default-run = "bit-to-byte"

[[bin]]
name = "udp-server"
path = "relay/server.rs"

[dependencies]
getrandom = "0.3"
crossterm = "0.29"
ratatui = "0.30.2"

# Prefer explicit error branches and named fields for members learning Rust.
[lints.clippy]
question_mark = "allow"
redundant_field_names = "allow"
needless_return = "allow"
single_match = "allow"
while_let_loop = "allow"
needless_bool_assign = "allow"
manual_find = "allow"
